import styles from "./CityList.module.css";

const CityList = () => {
    return (
        <ul className={styles.cityList}>
            <h2>city list</h2>
        </ul>
    );
};

export default CityList;
